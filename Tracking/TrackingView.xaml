<UserControl x:Class="WindowTrackerApp.TrackingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowTrackerApp"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid>
        <StackPanel>
        <TextBlock Text="{Binding TrackerVM.WindowTitle}" />
        <TextBlock Text="{Binding TrackerVM.WindowExecutable}" />
        <TextBlock Text="{Binding TrackerVM.WindowName}" />
        <TextBlock Text="{Binding ProjectName}" />
        </StackPanel>
    </Grid>
</UserControl>
